## Setup Instructions

1. Edit `data/stocks.json` to add your stocks
2. The workflow will automatically update prices and exchange rates
3. Open `index.html` in your browser to see the dashboard

## Technologies

- GitHub Actions for automation
- Free APIs (no API keys required)
- Plain HTML/CSS/JavaScript (no build required)
